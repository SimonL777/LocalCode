# 括号序列校验

## 题目

字符串只包含 `()`、`[]`、`{}`。当每个左括号都由同类型右括号按正确嵌套顺序闭合时返回 `true`，否则返回 `false`。

## 起始代码（不含题解）

### Java

```java
class Solution {
    public boolean isValid(String s) {

    }
}
```

### Python3

```python
class Solution:
    def isValid(self, s: str) -> bool:
        pass
```
