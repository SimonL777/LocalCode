# 数组配对

## 题目

给定整数数组 `nums` 和整数 `target`。返回两个不同元素的下标，使对应元素之和等于 `target`。每组输入保证恰好有一个答案。

示例：`nums = [2, 7, 11, 15]`、`target = 9`，返回 `[0, 1]`。

## 起始代码（不含题解）

### Java

```java
class Solution {
    public int[] twoSum(int[] nums, int target) {

    }
}
```

### Python3

```python
class Solution:
    def twoSum(self, nums: list[int], target: int) -> list[int]:
        pass
```
